# 포인트클라우드 기반 조류 판별 필터링 파이프라인

본 문서는 UGV(무인지상차량)에 탑재된 Livox Mid-360 LiDAR의 3D 포인트클라우드에서
공중 조류를 판별하기 위한 전처리 및 필터링 과정을 단계별로 설명합니다.

---

## 개요

LiDAR 센서는 주변 환경 전체를 3D 포인트 집합으로 반환합니다. 이 포인트들 중에는
지면, 벽, 로봇 본체, 나무 등 다양한 물체의 반사가 혼재되어 있습니다. 조류는 전체
포인트 중 극히 일부(공중, 소형, 움직임)에 해당하므로, 불필요한 포인트를 단계적으로
제거하는 과정이 필수입니다.

전체 파이프라인은 7단계 필터링과 칼만 필터 추적으로 구성됩니다.

```
Raw PointCloud2
    ▼ 1. 수평 거리 ROI 필터
    ▼ 2. 고도 임계값 필터
    ▼ 3. 자체 차량 포인트 제거
    ▼ 4. DBSCAN 클러스터링
    ▼ 5. 바운딩 박스 크기 필터
    ▼ 6. 클러스터 중심 고도 필터
    ▼ 7. 운동 연속성 필터
    ▼ 칼만 필터 추적 (+ YOLO 시각 폴백)
조류 탐지 확정
```

---

## 1단계: 수평 거리 기반 ROI 필터 (Range Filter)

### 설명

각 포인트의 수평 거리 r을 아래 수식으로 계산합니다.

    r = sqrt(x² + y²)

이 값이 0.3 m 미만이거나 15.0 m 초과인 포인트는 이후 처리 대상에서 제외합니다.
z(높이) 조건도 동시에 적용되며, 이 단계에서 z > 2.1 m 조건을 함께 검사합니다.

### 왜 사용하는가

- **근거리 제거 (< 0.3 m)**: LiDAR는 센서 바로 앞의 포인트에서 내부 반사, 렌즈 오염 등으로
  노이즈가 심합니다. 이 범위 안의 데이터는 신뢰하기 어렵습니다.

- **원거리 제거 (> 15.0 m)**: 조류처럼 작은 물체는 거리가 멀수록 반사되는 포인트가
  매우 희박해져 1~2개 포인트만 남습니다. 이 정도 데이터로는 클러스터를 만들 수 없고,
  오히려 노이즈로 분류될 가능성이 높습니다.

- **연산량 절감**: 관심 범위 밖을 초기에 잘라냄으로써 이후 클러스터링 등 무거운 연산의
  입력 포인트 수를 크게 줄입니다.

### 적용 파라미터

| 파라미터 | 값 |
|---|---|
| roi_min_range | 0.3 m |
| roi_max_range | 15.0 m |

---

## 2단계: 고도 임계값 필터 (Height Threshold Filter)

### 설명

포인트의 z 좌표(높이)가 2.1 m 미만이면 제거합니다. 즉, 센서 기준으로 2.1 m 위에
있는 포인트만 남깁니다.

### 왜 사용하는가

본 시스템의 운용 환경(UGV 시뮬레이션 월드)에는 높이 2.0 m의 벽이 존재합니다.
조류는 항상 공중에 있으므로 지면 및 벽 높이 이하의 포인트는 조류일 수 없습니다.

- **지면 포인트 완전 제거**: 가장 많은 포인트를 차지하는 지면 반사를 이 단계에서
  한 번에 제거합니다.

- **벽 상단 이하 구조물 제거**: 높이 2.0 m 벽의 포인트까지 제거하기 위해
  2.0 m + 0.1 m 여유 = 2.1 m 임계값을 설정합니다.

- **전체 파이프라인에서 가장 효과적인 단계**: 전체 포인트의 80~90%가 이 단계에서
  제거되므로, 이후 연산 부하가 극적으로 줄어듭니다.

### 적용 파라미터

| 파라미터 | 값 |
|---|---|
| ground_z_limit | 2.1 m |

---

## 3단계: 자체 차량 포인트 제거 (Self-body Exclusion Filter)

### 설명

LiDAR가 로봇 본체(카메라 마운트, 섀시 상단 구조물 등)를 직접 스캔하여 생기는
포인트를 제거합니다. 로봇 중심 좌표계에서 아래 박스 안에 속하는 포인트를 마스킹합니다.

    X: -0.6 m ~ +0.6 m
    Y: -0.6 m ~ +0.6 m
    Z: -0.3 m ~ +0.8 m

조건을 모두 만족하는(AND) 포인트가 이 박스 안에 속하며, 해당 포인트는 제거됩니다.

### 왜 사용하는가

LiDAR는 로봇 위에 장착되어 있습니다. 회전하는 LiDAR 빔이 카메라 틸트 장치, 배선
보호 커버, 섀시 프레임 등을 탐지하면 이 포인트들이 매 프레임마다 동일한 위치에
나타납니다. 이 포인트들을 제거하지 않으면 DBSCAN이 로봇 본체를 하나의 클러스터로
인식하여 조류로 오판할 수 있습니다.

- **z 하한 -0.3 m**: LiDAR 장착 위치 아래를 포함하여 섀시 전체를 커버합니다.
- **z 상한 +0.8 m**: 카메라 틸트 장치의 최대 높이를 포함합니다.
- **xy ±0.6 m**: 로봇 본체의 가로/세로 크기에 여유를 더한 값입니다.

### 적용 파라미터

| 파라미터 | 값 |
|---|---|
| self_x | (-0.6, 0.6) m |
| self_y | (-0.6, 0.6) m |
| self_z | (-0.3, 0.8) m |

---

## 4단계: DBSCAN 클러스터링

### 설명

DBSCAN(Density-Based Spatial Clustering of Applications with Noise)은
밀도 기반 군집화 알고리즘입니다. 서로 가까이 있는 포인트들을 하나의 클러스터(물체)로
묶고, 어느 클러스터에도 속하지 않는 포인트를 노이즈(-1)로 분류합니다.

동작 방식:
1. 임의의 포인트 p를 선택합니다.
2. p의 eps(0.6 m) 반경 안에 min_samples(3)개 이상의 포인트가 있으면 p는 코어 포인트입니다.
3. 코어 포인트와 밀도 연결된 모든 포인트를 같은 클러스터로 묶습니다.
4. 조건을 만족하지 못한 포인트는 노이즈로 분류됩니다.

노이즈로 분류된 포인트와, 포인트 수가 3개 미만인 클러스터는 모두 폐기합니다.

### 왜 사용하는가

- **비파라메트릭 형상 처리**: K-means와 달리 클러스터 개수를 미리 지정하지 않아도
  됩니다. 조류가 1마리일 수도, 여러 마리일 수도 있는 상황에 적합합니다.

- **노이즈 강인성**: 산발적으로 나타나는 노이즈 포인트는 자동으로 -1(노이즈)로 분류되어
  클러스터에 포함되지 않습니다.

- **3D 공간 적용 가능**: 포인트클라우드의 (x, y, z) 3차원 좌표를 그대로 입력으로
  사용하여 임의 형상의 3D 물체를 탐지할 수 있습니다.

- **eps = 0.6 m 설정 이유**: 조류의 크기(날개 포함 약 0.3~0.5 m)와 LiDAR 포인트 밀도를
  고려한 값입니다. 너무 작으면 한 마리의 새가 여러 클러스터로 쪼개지고,
  너무 크면 근접한 두 물체가 하나의 클러스터로 합쳐집니다.

### 적용 파라미터

| 파라미터 | 값 | 의미 |
|---|---|---|
| eps | 0.6 m | 이웃 포인트 탐색 반경 |
| min_samples | 3 | 코어 포인트 최소 이웃 수 |
| min_cluster_points | 3 | 유효 클러스터 최소 포인트 수 |

---

## 5단계: 바운딩 박스 크기 필터 (Bounding Box Size Filter)

### 설명

각 클러스터의 포인트들에 대해 축 정렬 바운딩 박스(AABB: Axis-Aligned Bounding Box)를
계산합니다.

    sx = x_max - x_min  (가로 크기)
    sy = y_max - y_min  (세로 크기)
    sz = z_max - z_min  (높이 크기)

아래 조건 중 하나라도 초과하면 그 클러스터는 조류가 아닌 큰 물체로 판단하여 폐기합니다.

    sx > 2.0 m  또는  sy > 2.0 m  또는  sz > 1.5 m

### 왜 사용하는가

조류는 작은 물체입니다. 2단계, 3단계 필터를 통과했더라도 높은 위치에 있는 벽 상단,
가로등 기둥, 수목 상단부 등은 여전히 클러스터로 만들어질 수 있습니다. 이런 구조물들은
조류보다 훨씬 크게 나타납니다.

- **sz < 1.5 m**: 조류의 수직 방향 크기는 LiDAR 기준으로 크게 측정되어도 1 m를
  넘기 어렵습니다. 1.5 m를 초과하는 클러스터는 조류가 아닌 대형 구조물입니다.

- **sx, sy < 2.0 m**: 가로 방향으로 2 m 이상인 클러스터는 조류 떼가 매우 조밀하게
  모여 있어도 현실적으로 초과하기 어려운 크기입니다.

### 적용 파라미터

| 파라미터 | 값 |
|---|---|
| trackable_max_size_x | 2.0 m |
| trackable_max_size_y | 2.0 m |
| trackable_max_size_z | 1.5 m |

---

## 6단계: 클러스터 중심 고도 필터 (Centroid Height Filter)

### 설명

클러스터의 무게중심(centroid) z 좌표가 2.0 m 미만이면 폐기합니다.

    centroid_z = mean(cluster[:, 2])
    if centroid_z < 2.0:  # 폐기

### 왜 사용하는가

2단계에서 개별 포인트 단위로 z > 2.1 m 를 필터링했지만, DBSCAN 클러스터링 이후에
클러스터 단위로 한 번 더 검증하는 이중 안전장치입니다.

예를 들어, z = 2.1 m 근방의 포인트들이 클러스터를 이루어도 그 중심이
2.0 m 아래라면 여전히 지면 근처 물체로 판단됩니다. 특히 벽 상단부처럼 높이가
경계선 근처에 있는 구조물을 추가로 걸러냅니다.

- **임계값 2.0 m**: 벽 최대 높이와 동일하게 설정하여 벽 위에 놓인 물체 반사를
  최종 단계에서 제거합니다.

### 적용 파라미터

| 파라미터 | 값 |
|---|---|
| trackable_min_centroid_z | 2.0 m |

---

## 7단계: 운동 연속성 필터 (Motion Continuity Filter)

### 설명

이전 단계까지 통과한 클러스터를 프레임 간 추적하면서 실제로 움직이고 있는지 확인합니다.
클러스터의 odom 프레임 좌표가 프레임 간 0.05 m ~ 3.0 m 범위로 이동한 것이
**3프레임 이상 연속**으로 확인된 경우에만 조류 후보로 확정합니다.

    if 0.05 m < 프레임 간 이동 거리 < 3.0 m:
        moving_count += 1
    if moving_count >= 3:
        is_moving_confirmed = True

**회전 동결(Rotation Freeze)**: 로봇이 회전 중일 때(|ω| > 0.20 rad/s) 이 판단을
일시 중단합니다.

### 왜 사용하는가

1단계~6단계를 모두 통과해도 높은 위치에 있는 정적 구조물(예: 전봇대 상단, 나무 꼭대기)의
클러스터는 남아 있을 수 있습니다. 이것들은 조류가 아니므로 움직이지 않습니다.
반면 조류는 반드시 움직입니다. 이 필터가 정적 물체와 조류를 최종적으로 구별합니다.

- **최소 이동 임계값 0.05 m**: 이보다 작은 변화는 LiDAR 측정 오차, 로봇 진동에 의한
  좌표 떨림으로 간주하여 무시합니다.

- **최대 이동 임계값 3.0 m**: 프레임 간 3 m 이상 이동은 물리적으로 불가능합니다.
  이를 초과하면 TF 변환 오차나 클러스터 교환(ID 스위칭) 오류로 판단합니다.

- **3프레임 연속 조건**: 1~2프레임만의 움직임은 일시적인 노이즈일 수 있으므로,
  3프레임 이상 지속된 경우에만 실제 움직임으로 인정합니다.

- **회전 동결 이유**: 로봇이 제자리 회전 중에는 odom 좌표계에서의 모든 물체 위치가
  변화합니다. 이를 물체의 실제 운동으로 오판하는 것을 막기 위해, 로봇 회전 속도가
  일정 임계값을 초과하면 이 프레임의 운동 카운트를 갱신하지 않습니다.

### 적용 파라미터

| 파라미터 | 값 |
|---|---|
| move_threshold | 0.05 m |
| max_motion_dist | 3.0 m |
| min_motion_frames | 3 프레임 |
| rotation_freeze_threshold | 0.20 rad/s |

---

## 추적 보완: 칼만 필터 (Kalman Filter)

### 설명

7단계 필터를 통과한 조류 후보 클러스터에 칼만 필터를 적용하여 위치를 추적합니다.

**상태 벡터**:

    x = [px, py, pz, vx, vy, vz]^T   (odom 프레임)

위치 3차원과 속도 3차원으로 구성됩니다.

**운동 모델** (등속 운동, Constant Velocity):

    px(t+dt) = px(t) + vx(t) * dt
    py(t+dt) = py(t) + vy(t) * dt
    pz(t+dt) = pz(t) + vz(t) * dt

**측정 갱신**: 매 프레임 LiDAR 클러스터의 무게중심 위치로 상태를 보정합니다.

**lookahead 예측**: 0.15 s 앞의 위치를 예측하여 로봇 제어 명령에 활용합니다.
이는 LiDAR 처리 → 클러스터링 → 제어 명령까지의 처리 지연을 보상합니다.

### 왜 사용하는가

- **LiDAR 유실 대응**: 조류가 LiDAR 블라인드 구역(센서 바로 위, 사각지대)에 진입하면
  클러스터가 검출되지 않습니다. 이때 최대 60 프레임(약 3초)까지 칼만 필터의 예측값만으로
  추적을 유지합니다.

- **위치 노이즈 평활화**: LiDAR 클러스터의 무게중심은 프레임마다 약간씩 흔들립니다.
  칼만 필터는 측정 노이즈(R)와 운동 모델 불확실성(Q)을 최적 결합하여 부드러운
  위치 추정값을 제공합니다.

- **속도 추정**: 클러스터 위치만으로는 속도를 직접 알 수 없습니다. 칼만 필터의 상태
  벡터에 속도가 포함되어 있어 조류의 현재 이동 방향과 속도를 추정할 수 있습니다.

**파라미터:**

    프로세스 노이즈 Q:
      위치 항:  0.5 * dt²  (속도 불확실성이 위치에 누적되는 효과)
      속도 항:  2.0 * dt   (새의 급격한 방향 전환 허용)
    측정 노이즈 R:
      xy 위치:  σ² = 0.25  (수평 측위 불확실성)
      z  위치:  σ² = 0.20  (수직 측위 불확실성, 약간 더 정밀)

---

## YOLO 시각 폴백 (Visual Fallback)

칼만 필터로도 추적을 유지하지 못하는 경우(60프레임 이상 유실), 카메라 기반 YOLO
탐지 결과로 방위각만 유지합니다.

- YOLO가 조류를 탐지하면 수평 방위각 오차를 `/bird_visual_bearing` 토픽으로 퍼블리시
- cluster_node가 이 방위각으로 로봇 회전만 수행 (거리 미확인이므로 전진 금지)
- LiDAR가 조류를 재탐지하면 즉시 LiDAR 모드로 복귀

이 메커니즘으로 LiDAR 단독 탐지의 한계를 카메라 센서로 보완하는 **센서 퓨전**을
구현합니다.

---

## 전체 파라미터 요약표

| 단계 | 파라미터 | 값 | 목적 |
|---|---|---|---|
| 1. ROI | roi_min_range | 0.3 m | 근거리 노이즈 제거 |
| 1. ROI | roi_max_range | 15.0 m | 원거리 희박 포인트 제거 |
| 2. 고도 | ground_z_limit | 2.1 m | 지면·벽 포인트 제거 |
| 3. 본체 | self_x/y | ±0.6 m | 로봇 본체 반사 제거 |
| 3. 본체 | self_z | -0.3~0.8 m | 로봇 수직 범위 |
| 4. DBSCAN | eps | 0.6 m | 클러스터 이웃 반경 |
| 4. DBSCAN | min_samples | 3 | 코어 포인트 기준 |
| 5. 크기 | max_size_x/y | 2.0 m | 대형 수평 구조물 제거 |
| 5. 크기 | max_size_z | 1.5 m | 대형 수직 구조물 제거 |
| 6. 중심고도 | min_centroid_z | 2.0 m | 저고도 클러스터 제거 |
| 7. 운동 | move_threshold | 0.05 m | 최소 이동 인정 거리 |
| 7. 운동 | max_motion_dist | 3.0 m | 최대 허용 이동 거리 |
| 7. 운동 | min_motion_frames | 3 | 연속 운동 인정 프레임 |
| 7. 운동 | rotation_freeze | 0.20 rad/s | 회전 중 판단 동결 임계값 |
| KF | kf_lookahead_sec | 0.15 s | 처리 지연 보상 예측 시간 |
| KF | max_missed_frames | 60 프레임 | 유실 허용 최대 프레임 |
