# Waver Maps

Store generated `map.yaml`, `map.pgm`, Cartographer `.pbstream`, or RTAB-Map `.db`
files here. Do not run outdoor patrol until the map, localization covariance, `/tf`,
and `/scan` are stable in the actual test area.
